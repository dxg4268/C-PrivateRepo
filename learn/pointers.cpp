#include<bits/stdc++.h>
using namespace std;


int main() {
    // Pointers --> data type which holds the address of other data types

    int a = 12;
    int* address = &a;
    cout << address;

    // std::cout << "Hello";
    return 0;
}